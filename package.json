{
  "dependencies": {
    "bootstrap": "^5.3.3",
    "sass": "^1.86.0"
  },
  "scripts": {
    "sass-watch": "sass --watch scss:css --no-source-map",
    "sass-build": "sass scss:css --style=compressed"
  }
}
